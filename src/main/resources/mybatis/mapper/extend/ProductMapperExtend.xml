<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suncaper.demo.mapper.ProductMapper">
    <insert id="MyInsertSelective" parameterType="com.suncaper.demo.entity.dto.ProductDto" useGeneratedKeys="true" keyProperty="id">
        insert into wcss_product(NAME, product_number, batch_id, gender, description) values
        (#{name},#{productNumber},#{batchId},#{gender},#{description})
    </insert>
</mapper>